// 1.find the last word
// 2. return last worth length
let text = "   fly me   to   the moon  ";
const lengthOfLastWord = (s) => {
  for (let i = s.length - 1; i >= 0; i--) {
    console.log(s[i]);

    if (char !== " ") {
      console.log(s[i]);
    }
  }
};
